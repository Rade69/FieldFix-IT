[project]
name = "fieldfix-it"
version = "0.1.0"
description = "FieldFix IT — Windows IT diagnostics and controlled repair tool"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.11",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
